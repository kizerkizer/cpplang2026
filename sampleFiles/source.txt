function foo (myParam: Integer): String {
    var baz = 'baz';
    var qux = 1053; // Some comment :-)
    while (true && !(12 == qux)) { // Should all typecheck OK to Boolean
        // Maybe this will be eventually detected as an unreachable area
        var b = 'Foo bar!';
        /*
         * Another comment (-:
         !
         */
    }
    qux = 25;
    if (myParam > 5) then {
        baz = 'bar';
    } else {
        baz = 'foo';
    }
    loop {
        /*if (foo) then { // Error: type error, `foo` is not assignable to `Boolean`
            break;
        }*/
        break;
    }
    //var qux = empty; // Error: redeclaration!
    return baz;
}

function main () {
    var someVariable = if true then 45 else 40; // Should eventually optimize to just 45
    var result: String = foo(45);
}