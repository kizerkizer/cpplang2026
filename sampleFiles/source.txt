function foo () {
    var baz = "baz";
    var qux = 1053; // Some comment :-)
    while (5) {
        log("Foo bar!");
        /*
         * Another comment (-:
         !
         */
    }
    return baz;
}

var lol = 12345 - -23 * 53;

var ccc = 5 + 10 + 12 + 3;

var qux = !80085 + 5 * !!!6;

var bar = 59 && 24 || 12 && 5;

function bbb () {
    return "bar" + lol + bar;
    while (35) {
        return "bar" || qux;
    }
}